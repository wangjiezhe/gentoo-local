diff --git a/CMakeLists.txt b/CMakeLists.txt
index 07ab6320fe9..408e4aeb2f3 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1490,12 +1490,6 @@ endif()
 # setuptools builds.
 include(cmake/FileMirroring.cmake)
 
-# Package data: install non-Python data files (type stubs, templates,
-# headers, etc.) into the torch package.  Previously declared via
-# setup.py package_data; now consolidated here for both scikit-build-core
-# and setuptools builds.
-include(cmake/PackageData.cmake)
-
 include(cmake/PostBuildSteps.cmake)
 
 include(cmake/Summary.cmake)
diff --git a/cmake/PostBuildSteps.cmake b/cmake/PostBuildSteps.cmake
index 71bd0438c38..726ca714bfd 100644
--- a/cmake/PostBuildSteps.cmake
+++ b/cmake/PostBuildSteps.cmake
@@ -21,7 +21,7 @@ install(CODE "
   execute_process(
     COMMAND \"${_python_exe}\"
       \"${_project_src}/tools/wrap_headers.py\"
-      \"\${CMAKE_INSTALL_PREFIX}/${TORCH_INSTALL_INCLUDE_DIR}\"
+      \"\${CMAKE_INSTALL_INCLUDE_DIR}\"
     COMMAND_ERROR_IS_FATAL ANY
   )
 ")
diff --git a/torch/CMakeLists.txt b/torch/CMakeLists.txt
index e2f0034cbdf..126dff6fa3b 100644
--- a/torch/CMakeLists.txt
+++ b/torch/CMakeLists.txt
@@ -465,9 +465,9 @@ if(BUILD_PYTHON AND NOT BUILD_LIBTORCH_WHL)
   # alongside the other import libs in torch/lib/, matching the layout that
   # setup.py used before _C moved to CMake.
   install(TARGETS _C
-    RUNTIME DESTINATION "."
-    LIBRARY DESTINATION "."
-    ARCHIVE DESTINATION "lib"
+    RUNTIME DESTINATION "${SKBUILD_PLATLIB_DIR}/torch"
+    LIBRARY DESTINATION "${SKBUILD_PLATLIB_DIR}/torch"
+    ARCHIVE DESTINATION "${SKBUILD_PLATLIB_DIR}/torch/lib"
   )
 
   # ---[ NCCL EP extension (torch._nccl_ep)
@@ -539,7 +539,7 @@ add_custom_target(
 add_dependencies(torch_python gen_torch_version)
 # version.py is gitignored (generated), so scikit-build-core won't include it
 # via Python package collection. Install it explicitly via cmake instead.
-install(FILES "${TORCH_SRC_DIR}/version.py" DESTINATION ".")
+install(FILES "${TORCH_SRC_DIR}/version.py" DESTINATION "${SKBUILD_PLATLIB_DIR}/torch")
 
 # _rocm_init.py is gitignored (injected by ROCm build scripts to bootstrap
 # torch/__init__.py). See https://github.com/ROCm/TheRock/blob/main/docs/packaging/python_packaging.md
