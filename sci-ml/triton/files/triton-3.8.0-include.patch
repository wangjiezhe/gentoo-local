diff '--color=auto' -ur triton-3.8.0.orig/setup.py triton-3.8.0/setup.py
--- triton-3.8.0.orig/setup.py	2026-08-28 21:55:54.000000000 +0800
+++ triton-3.8.0/setup.py	2026-09-12 10:22:32.166528302 +0800
@@ -255,6 +255,9 @@
         if cupti_include_dir == "":
             cupti_include_dir = os.path.join(get_base_dir(), "third_party", "nvidia", "backend", "include")
         cmake_args += ["-DCUPTI_INCLUDE_DIR=" + cupti_include_dir]
+        cuda_include_dir = get_env_with_keys(["TRITON_CUDART_PATH"])
+        if cuda_include_dir:
+            cmake_args += ["-DCUDA_INCLUDE_DIR=" + cuda_include_dir]
         rocm_include_dir = get_env_with_keys(["TRITON_ROCM_INCLUDE_PATH"])
         if rocm_include_dir == "":
             rocm_include_dir = os.path.join(get_base_dir(), "third_party", "amd", "backend", "include")
diff '--color=auto' -ur triton-3.8.0.orig/third_party/proton/CMakeLists.txt triton-3.8.0/third_party/proton/CMakeLists.txt
--- triton-3.8.0.orig/third_party/proton/CMakeLists.txt	2026-08-28 21:55:54.000000000 +0800
+++ triton-3.8.0/third_party/proton/CMakeLists.txt	2026-09-12 10:22:18.674532760 +0800
@@ -6,6 +6,9 @@
 set(PROTON_COMMON_DIR "${CMAKE_CURRENT_SOURCE_DIR}/common")
 
 # ============ Check for includes =============
+if(NOT CUDA_INCLUDE_DIR)
+  message(FATAL_ERROR "CUDA include directory not defined")
+endif()
 if(NOT CUPTI_INCLUDE_DIR)
   message(FATAL_ERROR "CUPTI include directory not defined")
 endif()
@@ -38,6 +41,7 @@
 
   target_include_directories(${name}
     PRIVATE
+      "${CUDA_INCLUDE_DIR}"
       "${CUPTI_INCLUDE_DIR}"
       "${JSON_INCLUDE_DIR}"
       "${PROTON_COMMON_DIR}/include"
